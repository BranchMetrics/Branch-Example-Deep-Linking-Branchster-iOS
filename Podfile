use_frameworks!
platform :ios, '9.0'

target 'BranchMonsterFactory' do
    pod 'FBSDKCoreKit'
    
    # For app clip support, we're switching to SPM 
    #pod 'Branch', :path => '~/github/ios-branch-deep-linking-attribution'
    #pod 'Branch'
end
